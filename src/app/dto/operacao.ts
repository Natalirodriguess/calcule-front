export class Operacao {

  operadorUm: number;
  operadorDois: number;

  constructor(operadorUm: number, operadorDois: number) {
    this.operadorUm = operadorUm;
    this.operadorDois = operadorDois;
  }

}
